[STUDYLION]
token = 

[DATA]
args = dbname=lion_data
appid = StudyLion

[TOPGG]
auth =

# --- AI-MODIFIED (2026-04-30) ---
# Purpose: HTTP listener for the website's "Feature Your Server" feature.
#   Loads on shard 0 only. The website calls these endpoints to:
#     POST /listing/invite/create  -- create / rotate a Discord invite
#     POST /listing/notify         -- post a review embed to the moderator channel
#   `auth` MUST match the BOT_HTTP_SHARED_SECRET environment variable on
#   Vercel for the LionBot website. The website sends "Bearer <auth>".
[SERVERLISTING]
enabled = false
port = 7001
auth =
# --- END AI-MODIFIED ---
